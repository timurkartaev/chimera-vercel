import logging
from ..integration_connector.capabilities.entity import EntityCapability

logger = logging.getLogger(__name__)

class PipedriveEntityCapability(EntityCapability):
    """
    Pipedrive entity capability that extends the base entity capability.
    Currently uses all base functionality without modifications.
    """
    pass 